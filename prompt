Migrate my NixOS desktop shell from illogical-impulse (quickshell/ii) to Noctalia v5
(C++/native) on the niri compositor. The decision and rationale are already settled —
read the memd brief for full context (resource tax, color-engine integration, niri/quake
findings).

Start with the worst-case reconnaissance, change nothing live:
1. Pull Noctalia v5 (`main` branch) source + docs. Report what its current config/settings
   surface covers vs. my used bar widgets: workspaces, cpu/mem/temp, media (playerctl),
   system tray, clock+date, weather, network.
2. Critically: what did v5 replace the v4 sidebars / attached bar panel with? It's alpha —
   v4 plugins are broken, the panel is gone, some UI dropped. I need to know the sidebar
   story before committing cycles.
3. Confirm the nix `colors` palette option is wired so my apply_theme.py can drive it.

Then set up niri + Noctalia v5 as a SECOND NixOS session (declarative, reversible), leaving
my Hyprland/ii session live as fallback (v4.7.7 pinned as backup). Burn no boats.
