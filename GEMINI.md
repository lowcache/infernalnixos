## Workspace Rules

This repository outlines a declarative Nix configuration layout. 

1. When working with paths containing `.nix` structures, you must prioritize the local `.agents/skills/nix-expert/` capability.
2. Ensure you invoke the `/format_nix_file` command to sanitize configuration adjustments immediately following any file output or modification step.
